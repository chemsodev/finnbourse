/**
 * Shared types for agence components
 */

// User type for the related users table
export type RelatedUser = {
  id: string;
  fullName: string;
  position: string;
  role: string;
  status: string;
  organization: string;
};
